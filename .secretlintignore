# Files with intentional non-secret dev tokens or example credentials
# whiteboard-dev is a well-known loopback-only dev token, not a real secret
.claude/settings.json
.codex/config.toml

# Dependency lockfiles contain package registry tokens and hashes
pnpm-lock.yaml
package-lock.json
yarn.lock

# Build output and caches
node_modules/
dist/
.cache/
tmp/

# Environment variable example files (intentional placeholders)
.env.server.example
**/.env.example
**/.env.sample

# Test files use example credentials and fixture paths (e.g. http://user:pass@host,
# /Users/alice/ as representative input data) by design — they are not secrets.
**/*.test.ts
**/*.test.tsx
**/*.test.mjs
**/*.test.js
**/*.spec.ts
**/*.spec.tsx
tests/

# This file defines regex patterns about Unix paths as code — the comment examples
# like "/Users/me/project" are illustrative, not real credentials.
packages/mcp-server/src/shared/diagnostics/redact.ts
