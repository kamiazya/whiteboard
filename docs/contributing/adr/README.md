# Architecture Decision Records

This directory contains Architecture Decision Records (ADRs) for this project. ADRs capture significant technical decisions, their context, and their consequences so future contributors can understand why things are the way they are.

## When to write an ADR

Write an ADR when a decision:

- Affects the architecture, public API, or cross-cutting contracts (schemas, transports, persistence formats).
- Will be difficult or expensive to reverse.
- Could surprise a contributor reading the code without context.
- Resolves a genuine tradeoff between competing approaches.

Do not write an ADR for routine implementation choices, style preferences, or decisions with a single obvious answer.

## Numbering

ADRs are numbered sequentially: `ADR-0001`, `ADR-0002`, etc. The filename follows the pattern `NNNN-short-title.md`. Numbers are never reused, even if an ADR is superseded.

## Status lifecycle

| Status | Meaning |
|---|---|
| **Proposed** | Under discussion; not yet adopted. |
| **Accepted** | Adopted; the decision is in effect. |
| **Superseded** | Replaced by a later ADR (link to the successor). |

## Template

See [template.md](template.md) for the standard structure (MADR-lite: Title, Status, Context, Decision, Consequences, Alternatives considered).

## Index

| ADR | Title | Status |
|---|---|---|
| [ADR-0001](0001-apps-web-canonical-frontend.md) | apps/web as the canonical frontend | Accepted |
| [ADR-0002](0002-browser-to-daemon-transport.md) | Browser-to-daemon transport for Stage 4 | Accepted |
| [ADR-0003](0003-track-claude-dev-flow-tooling.md) | Track .claude AI dev-flow tooling in git with repo-relative workflow scriptPaths | Accepted |
| [ADR-0004](0004-unified-capability-gated-canvas-page.md) | Unified capability-gated CanvasPage | Accepted |
| [ADR-0005](0005-hosted-origin-authorization.md) | Authorizing a hosted origin against the local daemon | Accepted — not yet implemented |
| [ADR-0006](0006-object-oriented-ui.md) | Object-oriented UI — create from the palette, act from the object | Accepted |
| [ADR-0007](0007-canvas-identity-and-store-split.md) | Canvas identity and the daemon's two-store split | Accepted — says *slug* for what is now *path* |
| [ADR-0008](0008-slug-derivation-and-rename.md) | Slug derivation, rename, and sibling uniqueness | Accepted — read *slug* as *path* |
| [ADR-0009](0009-mcp-tool-naming.md) | The Document model, and `wb_<entity>_<action>` tool naming | Accepted — decisions 3-4 now implemented; *format* is spelled `kind` |
| [ADR-0010](0010-canvas-edit-batch-tool.md) | One batch tool for spatial editing, and why it is not `apply` | Accepted |
| [ADR-0011](0011-font-distribution.md) | Fonts are installed by whoever needs them, not bundled | Accepted — provider registry not built yet |
| [ADR-0012](0012-user-installed-fonts.md) | A user installs a font by naming it, and the daemon keeps it | Accepted — not yet implemented; extends ADR-0011 |
| [ADR-0013](0013-facet-system.md) | The facet system — plugins, versioned facet keys, and the meaning/display split | Accepted — key grammar landed; supersedes ADR-0009 decision 3 |
| [ADR-0014](0014-reference-index.md) | Cross-document references are a derived projection with an event-fed aggregate | Accepted |
| [ADR-0015](0015-search-quality-scoreboard.md) | A judged corpus decides whether search needs embeddings | Accepted |
| [ADR-0016](0016-okf-trust-family.md) | OKF v0.2 trust family: a declared actor, a server-stamped time, and a bucket of its own | Accepted — daemon write path only |
| [ADR-0017](0017-okf-bundle-mapping.md) | Mapping a workspace onto an OKF bundle | Proposed — design only |
| [ADR-0018](0018-operation-vs-mechanic.md) | An operation is a use case; the composition root holds only mechanics | Proposed |
| [ADR-0019](0019-workspace-identity.md) | Workspace identity is three layers, in both keepers | Proposed |
| [ADR-0020](0020-coordination-boundary.md) | The coordination boundary — a CRDT data plane and a compare-and-swap control plane | Proposed |
| [ADR-0021](0021-durability-boundary.md) | Durability is a property of each store, not an operation on a directory | Accepted (implemented) |
| [ADR-0022](0022-variation-addressing.md) | A variation is not in the address, and the default one has no name to put there | Accepted — decision 1's grammar rule stands; the variation surface it addresses is retired by ADR-0029 |
| [ADR-0023](0023-replica-model.md) | A workspace has one keeper; every other copy is a replica | Accepted — implemented (verified demote deletes the browser copy; replicas serve offline reads) |
| [ADR-0024](0024-canvas-comments.md) | Canvas comments are a first-class annotation layer, keyed per comment | Accepted |
| [ADR-0025](0025-comment-editor-ux.md) | Comment editor UX: context-menu create, resolved toggle, authorless v1, pull-by-convention AI delivery | Accepted |
| [ADR-0026](0026-annotation-layer.md) | The annotation layer — one plane per document, threads, and selector anchors | Proposed — design of record for comments beyond the canvas |
| [ADR-0027](0027-render-broker.md) | Every picture of a document goes through one broker, and the cache is a memo | Accepted — in-tab broker landed; OPFS persistence and the SharedWorker implementation are follow-ups |
| [ADR-0028](0028-quiet-persistence.md) | The routine save state is not shown; the shell mark speaks only for a condition | Accepted — the browser page's save chip is removed, the shell mark carries both keepers' health; writing at once (no debounce window) is the named follow-up |
| [ADR-0029](0029-proposal-layer.md) | A proposal is an anchored change, not a point in time | Accepted — design of record; nothing implemented yet. Retires the variation surface of ADR-0022 |
| [ADR-0030](0030-render-theme.md) | A render theme is a document's pen and paper — a canvas facet naming a registered asset | Accepted — design of record (human gate 2026-09-09); nothing implemented yet. Extends ADR-0013, binds canvas-render decision #10 |
| [ADR-0031](0031-tool-surface-criteria.md) | The tool surface is judged by an instrument, not by its count | Accepted — criteria, the pinned tool-surface scoreboard and the opt-in LLM-driven lane land together; the retirements it points at are follow-ups |
| [ADR-0032](0032-composition-axis.md) | A composition axis — what a drawing hands its reader, judged by four principles with their sources | Accepted — criteria, columns, validity claim, and the score and pinned scoreboard that land with them. Sits beside ADR-0031 §7's drawing score |
| [ADR-0033](0033-facet-vocabulary-axis.md) | A facet-vocabulary axis — whether what a reader can SEE matches what the document DECLARES | Accepted — criteria, columns, validity claim, and the score and pinned baseline that land with them. Sits beside ADR-0031 §7 and ADR-0032, and takes over the question ADR-0032's `contrast` reserved |
| [ADR-0034](0034-stencil-and-recipe.md) | Stencil and recipe — a reusable element and a reusable arrangement, as two concepts | Proposed — the vocabulary, the boundaries (a recipe never emits coordinates; a library is data, not a distribution-time schema), the granularity criterion, and the measurement plan. Stencils land first |
| [ADR-0035](0035-device-keys-and-keeper.md) | A key belongs to a device, and a server keeps rather than vouches | Accepted. Decisions 1-2 implemented for the daemon (a `did:key` it derives, advertises and stamps as a version's actor); checkpoint signatures, sharing, E2EE and the user DID's method deferred with triggers. Decision 1's scope narrowed to the daemon's unattended key by ADR-0039 (2026-09-16 addendum) — the browser holds no keypair. Extends ADR-0016's self-report admission and ADR-0023's keeper model |
| [ADR-0036](0036-semantic-axes.md) | A semantic axis is declared, and a channel carries at most one | Accepted, and carried out — the instrument, the declaration, and the bundled stencils' move off the colour channel. Extends ADR-0033's columns and constrains what ADR-0034's stencil may spend |
| [ADR-0037](0037-model-and-format.md) | The document model is native; JSON Canvas and OKF are projections | Accepted — human gate 2026-09-11; stands on the schema census in `packages/codec`, landing in dimension-sliced increments |
| [ADR-0038](0038-ocif-projection.md) | OCIF is a third projection, and its decompositions discipline the native model | Accepted — human gate 2026-09-12; stands on the OCIF ledger's measurement (native 15 against JSON Canvas's 21, and `degraded` 4 → 11, which is the format HAVING distinctions this model lacks). Splits relation from drawn line, and makes text a resource |
| [ADR-0039](0039-passkey-attestation.md) | A passkey attests a person's presence, not a device | Accepted — human gate 2026-09-16. Promotion attestation implemented end to end (#1610 daemon and contracts, #1612 browser and History, #1614 real-browser smoke); the user-verification gate (decision 6, refined 2026-09-17 to a per-workspace gate at the switch) and the profile (decision 9) is now [ADR-0041](0041-profile-and-authority.md); the gate itself remains design. The browser stores no private key (the v5→v6 removal generalised); a row's human-reviewed claim comes from the OPERATION (`kind: 'human'`, not `auto`), and a WebAuthn assertion is evidence beside it, asked for where an agent forging the click would matter (promote always, proposal approval by setting, save never). Amends ADR-0035 decision 1's scope on the measured fact that a relying party cannot make a credential device-bound |
| [ADR-0040](0040-scoped-tags.md) | Scoped tags are the one classification vocabulary, on documents, boards, nodes and edges | Accepted — human gate 2026-09-16; design of record, increments 1-4 to follow. `key:value` tags on markdown documents, spatial documents, nodes and edges (an infrastructure link is healthy or failing too; lines, being ink, get none); several values under one key allowed, with what search, the score (`multi`), the legend and the editor each do with them; `semantic.class/v0` retires unshipped; management as an in-use listing with rename/merge now and a declared library later. Supersedes ADR-0009 decision 3's spatial gap and ADR-0036 §6; gives ADR-0033 its `tags` reading |
| [ADR-0041](0041-profile-and-authority.md) | A profile is a per-keeper claim, and authority reaches resources but not identity | Accepted — human gate 2026-09-19; S8 slices 1-3 implemented (the `workspaceAccess` gate now reaches the replica-key route and every other online route in local-daemon mode, with a dedicated removed-member page and a once-only passkey-bind retry for a session that has not bound one yet). Completes ADR-0039 decision 9's profile. Each keeper may hold a profile record (a locally minted identifier, a display name, the credential ids it accepts); an authority order decides presentation and reconciliation only. Revocation is three layers — L1 access to a resource and L2 acceptance of a credential belong to the keeper (L2 ships: #1610 routes, #1627 screen), L3 the person's existence belongs to nobody, which is ADR-0035 decision 4's issuer line. The order is per-workspace configuration; the right to `demote` follows resource ownership rather than authority rank, so a personal server can be wound down and an organisation's workspace cannot be walked off with. Rides ADR-0023's keeper model rather than adding one |
| [ADR-0042](0042-offline-revocation.md) | An offline policy decides whether a replica exists, and revocation withholds its key | Accepted — human gate 2026-09-19, in effect since 2026-09-21. An organisation states per workspace whether its workspaces may be replicated at all (`no-offline` / `offline` / `bounded`), and where a replica exists it is ciphertext under a per-workspace key the client holds IN MEMORY ONLY — persisting it collapses the design to the advisory case, which is `browser-idb.ts`'s v5→v6 `reconnectKeypairs` lesson again. IMPLEMENTED: the tier is enforced at the daemon's `POST /api/workspaces/:id/replica-key` route, and every production `apps/web` `DocumentStore` construction seals a daemon-kept workspace's IndexedDB replica under the session key through one factory. Revocation withholds the key and that is the whole mechanism: nothing is deleted, so deleting is housekeeping rather than security and a reversed revocation costs nothing. A revoked actor's un-merged offline edits are discarded with the rest — the organisation owns them — though the person is told. NOT E2EE (the keeper holds the plaintext either way), correcting an earlier framing that made the bounded tier wait on it. Cold-start offline is still deferred to `prf` wrapping, one gesture with ADR-0039 decision 6's gate |
| [ADR-0043](0043-authority-as-keys.md) | Authority is a key, delegation derives a narrower one, and revocation is declining to hand out the next | Accepted — in effect since 2026-09-20 by increment: the ACT plane ships as enforcement only (#1647 core, #1651 one credential resolver, #1653 `/mcp`, #1648/#1666 the root key's posture), and nothing in production mints a macaroon; the READ plane (decision 3) shipped 2026-09-21 — the daemon mints a per-workspace content key and `apps/web` seals a daemon-kept workspace's IndexedDB replica under it, held in memory only for the tab's life; `prf`-wrapped cold-start-offline unlock (ADR-0042 decision 6) is still deferred. Replaces a withdrawn vocabulary-only draft that deliberately chose no mechanism (its own closing paragraph named that defect and it shipped anyway). TWO PLANES, not equally strong, and the split is ADR-0035 decision 1's own ("a user-level key … does a different job"): the **act** plane is a token the daemon checks, so a bug in that check gets past it; the **read** plane is a content key, so arithmetic stops a holder whatever its disk holds — the only plane ADR-0042's "cryptographic rather than advisory" covers. Read authority attenuates by a specified HKDF tree over ADR-0042's workspace key — which 0043 AMENDS from "the key the bytes are under" to "the root the per-document key is derived from", since otherwise a document key opens nothing — with a per-document `epoch` so revoking one document does not re-key the workspace; act authority by a macaroon-style HMAC chain (caveats as Zod predicates, not Datalog), implemented natively over WebCrypto in `security/macaroon.ts` rather than imported — libmacaroons' published vectors turned out to be SERIALIZATION vectors for its own wire formats, so the chain is pinned by committed golden signatures instead, a weaker check the module says so about. Biscuit is the named upgrade on two triggers — a verifier that must not be able to mint, or a third party in a caveat. UCAN and ZCAP-LD are rejected on a measured fact, not taste: both need the delegation's AUDIENCE to hold a signing key, and `browser-idb.ts`'s v5→v6 removal took that away deliberately. Act-plane revocation is bounded by a token lifetime and is NOT offline-verifiable — where a revocation must bite an offline holder it is ADR-0042's withheld key that bites, not this; a deny-list is rejected as an ACL wearing a capability's clothes. Attenuation is NOT new here — an OAuth grant is scope-checked on HTTP and its WS ticket carries `redeemed.scopes`; the daemon token and the origin-bound pairing token were the two that were unnarrowed, and the browser/pairing surface now is. Decision 9's first application (the agent's reach) is WITHDRAWN by the 2026-09-20 addendum: no shipped agent path reaches the daemon with a credential — `.mcp.json` runs the server in-process over stdio — so an issued token would have no holder, and the act plane rests at enforcement until one exists. The second application, the content-key tree, is untouched and next |
