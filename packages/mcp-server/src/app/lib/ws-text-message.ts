// Relocated to src/shared/ws-text-message.ts as part of the daemon-backend
// transport move. Re-exported here so existing src/app imports keep working
// unchanged.
export { parseServerTextMessage } from '../../shared/ws-text-message.js'
