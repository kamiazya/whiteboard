{
  "name": "@kamiazya/whiteboard-search",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "description": "Lexical search over documents: a dictionary-free tokenizer (latin words, CJK bigrams), BM25 ranking, match snippets, and the one definition of what text a document contributes. Pure and runtime-agnostic, so the daemon and the browser rank the same corpus the same way.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.node.config.ts"
  },
  "dependencies": {
    "@kamiazya/whiteboard-model": "workspace:*"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.4.1",
    "fast-check": "^4.7.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
